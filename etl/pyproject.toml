[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "etl"
version = "0.1.0"
description = "ETL para la generación de escenarios de simulación en SUMO"
requires-python = ">=3.11"
dependencies = [
]

[tool.setuptools.packages.find]
where = ["src"]
